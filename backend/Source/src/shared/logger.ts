/**
 * A super small wrapper around console.log
 * Replace this,if logging should be done in a different way (e.g. GreyLog)
 * @param all The data to log.
 */
export function log(...all) {
  console.log(all);
}
